# 2026-03-30 工作记录

## 菜单全面审查与修复
- 全面审查系统菜单所有项，发现 7 个问题并全部修复：
  1. **import_file 死事件** — 原来只分发无人监听的 menu-import-file 事件。改为直接调用 open() 文件选择 + invoke('import_file') 读取内容 + 分发 editor-import-content 事件。MarkdownEditor 新增对应监听器插入文本
  2. **⌘L 快捷键冲突** — select_line(编辑) vs toggle_layout(视图)。移除 select_line 的快捷键
  3. **⌘J 快捷键冲突** — line_join(编辑) vs toggle_chat(视图)。移除 line_join 的快捷键
  4. **⌘H 快捷键冲突** — find_replace(编辑) vs version_history(视图)。移除 find_replace 的快捷键（⌘H 在 macOS 原生语义为隐藏窗口）
  5. **新建专业文档无提示** — new_document_dialog 无项目时静默失败。添加 message 提示
  6. **工具菜单无处理** — tools_quick_capture 和 tools_ebook_reader 落入 default。添加独立 case 显示"功能开发中"
  7. **死代码清理** — 删除 handleProjectRename 和 handleDocRename（已改由 FileTree 行内处理）
- 修改文件：main.rs、useMenuEvents.ts、MarkdownEditor.tsx
