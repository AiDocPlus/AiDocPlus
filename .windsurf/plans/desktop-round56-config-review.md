# Desktop Round56 计划

## 目标

- 继续推进 Rust 阶段 4，聚焦 `apps/desktop/src-tauri/src/config.rs`。
- 审计其中的宽泛抑制、死代码痕迹与可抽离的稳定路径/配置 helper。
- 保持配置初始化、路径解析和状态行为不变。

## 执行步骤

1. 阅读 `config.rs` 并定位 `allow`、未使用项、重复逻辑。
2. 选择一组最稳的低风险目标做收口。
3. 保持外部状态结构与路径规则不变。
4. 执行 `cargo check` 验证。

## 约束

- 不改持久化位置。
- 不改状态结构。
- 不做无证据删除。
