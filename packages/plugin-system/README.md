# @aidocplus/plugin-system

此目录为 **npm 包占位 / 未来扩展预留**。内置插件与 Plugin SDK 的**实际源码**在主程序仓库中维护：

- **插件与 SDK**：`apps/desktop/src-ui/src/plugins/`（各 `{name}/` 插件 + `_framework/` SDK）
- **架构说明**：见主仓库根目录 `CLAUDE.md` 中的「插件架构」与「当前插件」章节
